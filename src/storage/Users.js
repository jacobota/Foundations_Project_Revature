//list to hold employees and managers
//will switch this to DynamoDB when learned
const employeeList = [];
const managerList = [];

//variable to hold the current user
const currentUser = [];

module.exports = {
    employeeList: employeeList,
    managerList: managerList,
    currentUser: currentUser
}